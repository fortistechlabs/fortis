<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider (${applicationId}.fileprovider) — exposes only cacheDir/shared/,
     where the QR "copy image" feature writes qr.png. -->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
